{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "functions": {
    "api/comments.js": {
      "runtime": "nodejs18.x"
    }
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" },
    { "source": "/(.*)", "destination": "/" }
  ]
}
